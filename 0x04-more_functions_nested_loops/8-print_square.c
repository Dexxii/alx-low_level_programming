#include "main.h"

/**
 * print_square - prints a square
 * @size: size of the same
 */
void print_square(int size)
{
	if (size <= o)
	{
		_putchar('\n');
	}
	else
	{
		int i, j;

		for (i = 0; i < size; i++)
		{

			for (i = 0; j < size; j++)
			{
			_putchar('#');
			}
		_putchar('\n');
		}
	}
}
